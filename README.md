My first linux remote scrypt 

This scrypt works by using ruby and nginx

How to start:

<code> bundle install

bundle exec ruby deploy/deploy.rb --user=USER_NAME --passwd=PASSWORD --host=HOST_ID</code>

